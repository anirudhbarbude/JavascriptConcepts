// let word = "anirudha";
// let wordToFind = 'a';
// let indexes = [];

// for (let i = 0; i < word.length; i++) {
//     if (word[i] === wordToFind) {
//         indexes.push(i);
//     }
// }

// console.log("Indexes of '" + wordToFind + "': " + indexes.join(', '));



let word = "nikhil";
let charToFind = 'i';

for (let i = 0; i < word.length; i++) {
    if (word[i] === charToFind) {
        console.log(`Index of '${charToFind}' found at position ${i}`);
    }
}
